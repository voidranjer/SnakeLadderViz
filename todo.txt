Revert to previous search path if clicking on a previous index
Use actual graphics for snakes and ladders (or use different colors, at least)
Undo button
Reset function and reset button (auto reset after rickroll)
Use localstorage to store hasRickrolled as to not spam the user

scale ladder width etc with grid size
hide ladder when hovering valid

load from file